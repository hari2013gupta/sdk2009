import 'package:flutter/material.dart';

class PayuView extends StatelessWidget {
  const PayuView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
