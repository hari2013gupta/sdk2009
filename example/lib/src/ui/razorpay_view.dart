import 'package:flutter/material.dart';

class RazorpayView extends StatelessWidget {
  const RazorpayView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
