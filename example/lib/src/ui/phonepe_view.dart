import 'package:flutter/material.dart';

class PhonePeView extends StatelessWidget {
  const PhonePeView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
